name: flutter_color_interview_task
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1
dependencies:
  color_repository:
    path: packages/color_repository
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  provider: ^6.1.5+1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.3

flutter:
  uses-material-design: true