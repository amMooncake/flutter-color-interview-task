name: color_repository
description: "The Repo that handles color generation"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.1
dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.3

flutter:
  uses-material-design: true